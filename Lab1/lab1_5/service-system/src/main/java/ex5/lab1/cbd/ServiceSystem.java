package ex5.lab1.cbd;

/**
 * Hello world!
 *
 */
public class ServiceSystem {
    public static void main(String[] args) {

        while (true) {
            System.out.print("username: ");
            System.out.println("product");
            // Pede o username e o produto
            // verifica se o numero de entradas para esse username já é 30
            // senão, adiciona o par (username, product), não tem mal se o produto é repetido, apenas subscreve o anterior (?)
            // usar um timer para, ao final desse tempo, zerar as entradas do utilizador x
        }
    
    }
}
